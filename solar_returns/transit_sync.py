# solar_returns/transit_sync.py

class TransitSync:
    """
    Synchronizes transits
    with progression charts.
    """

    def synchronize(
        self,
        transit_chart,
        progression_chart
    ):
        return {
            "synchronized": True
        }
